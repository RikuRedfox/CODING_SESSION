#include <iostream>

int main() {
       //sides of the room
       double length;
       double width;
       std::cin >> length;
       std::cin >> width;

       //output the area
       std::cout << (length * width);
       
	return 0;
}